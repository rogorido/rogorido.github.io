---
import Layout from "@/layouts/Layout.astro";
import { skills } from "../data/cv";

import ListSkills from "@/components/ui/ListSkills.astro";
import { seo } from "@/settings";
---

<Layout title={`Skills | ${seo.default_title}`}>
  {skills.length > 0 && <ListSkills listTitle="Skills" listItems={skills} />}
</Layout>
